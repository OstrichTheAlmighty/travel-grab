# Duffel API key — required for flight search
DUFFEL_API_KEY=

# OpenAI API key — optional; enables AI-generated flight explanations
# Falls back to deterministic explanations if missing or on error
OPENAI_API_KEY=
